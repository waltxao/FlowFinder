# v0.6.5 仿访达 UI 重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v0.6.4 用户反馈的 8 个 UI 问题（收藏夹、标签药丸、操作区卡片、路径栏、工具按钮、单击选中、应用图标、缩略图），并完成全量 UI 巡检。

**Architecture:** 三层分阶段推进：第一层重构 MainWindowController 布局结构（移除圆角卡片 + 双行工具栏），第二层修复 5 个组件级问题（收藏夹仿访达、药丸裁切、选中渲染、图标圆角、缩略图链路），第三层对照访达标准巡检 14 个 UI 窗口/面板。纯 UI 层修改，不动 Rust Core / Bridge。

**Tech Stack:** Swift 5.9 + AppKit（NSTableView/NSOutlineView/NSStackView/NSSplitView）、NSVisualEffectView、QLThumbnailGenerator、SF Symbols。Xcode-beta 构建。

## Global Constraints

- 必须使用 Xcode-beta 构建：`export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer`
- 项目路径：`/Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native`
- 构建：`xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj -scheme FlowFinderNative -configuration Debug build`
- 所有 UI 文案和代码注释使用简体中文
- 主题切换保持 light/dark 二态（无 system 自动跟随）
- 保留 FlowFinder 特性：双面板、侧边栏品牌区、查重/AI 工具、divider 悬停高亮
- 每个修复点用任务编号注释（延续 F1/R1 惯例）
- 选中样式：访达标准（获焦强调蓝+白字，失焦灰）
- 对齐基准：macOS 原生访达（不再绑定"精修版"设计稿）

---

## 文件结构

### 将要修改的文件

| 文件 | 职责 | 本轮改动 |
|------|------|---------|
| `UI/MainWindowController.swift` | 主窗口布局 | 移除操作区圆角卡片、重构双行工具栏布局、修复选中渲染配套 |
| `UI/PaneToolbar.swift` | 操作区工具栏 | 接受 BreadcrumbBar 嵌入 Row1、调整内部约束 |
| `UI/BreadcrumbBar.swift` | 路径栏 | 适配嵌入 PaneToolbar Row1 |
| `UI/SidebarView.swift` | 侧边栏 | 收藏夹全面仿访达、应用图标圆角 |
| `UI/FileListView.swift` | 文件列表 | 标签药丸裁切修复、缩略图调用链修复、选中渲染修复 |
| `Bridge/ThumbnailManager.swift` | 缩略图管理 | 增加日志、修复回调线程 |

### 不修改的文件

- `rust-core/` 整个目录
- `Bridge/CoreBridge.swift`、`Bridge/FFIFunctions.swift` 等 Bridge 层
- `Model/` 数据模型层

---

## 第一层：布局结构重建

### Task 1: 移除操作区圆角卡片（问题3）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:379-509`（createPaneContainer）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:279-282`（旧注释清理）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:318-326`（移除 applyPaneBackgroundColor 调用与主题监听）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:511-527`（移除 applyPaneBackgroundColor 方法）

**Interfaces:**
- Consumes: 无
- Produces: paneContainer 无圆角、透明背景，让 NSVisualEffectView 透出

- [ ] **Step 1: 移除 createPaneContainer 中的圆角和裁切**

打开 `UI/MainWindowController.swift`，定位 `createPaneContainer` 方法（约 379 行）。将以下代码：

```swift
container.wantsLayer = true
container.layer?.backgroundColor = NSColor.clear.cgColor
// 任务 R1: 操作区圆角 12pt 卡片
container.layer?.cornerRadius = 12
container.layer?.masksToBounds = true
```

替换为：

```swift
container.wantsLayer = true
container.layer?.backgroundColor = NSColor.clear.cgColor
// 任务 F2: 移除圆角卡片，仿访达撑满（v0.6.5）
// 操作区背景透明，让 NSVisualEffectView 的 .underWindowBackground 材质透出
container.layer?.cornerRadius = 0
container.layer?.masksToBounds = false
```

- [ ] **Step 2: 移除 applyPaneBackgroundColor 方法及其调用**

定位 `applyPaneBackgroundColor` 方法（约 511-527 行），整段删除。

定位 `setupWindow`/`loadView` 中调用 `applyPaneBackgroundColor()` 的位置（约 319 行），删除该行。

定位主题监听块（约 321-326 行）：

```swift
// 监听主题变更，刷新操作区背景色
ThemeManager.shared.onModeChanged = { [weak self] _ in
    DispatchQueue.main.async {
        self?.applyPaneBackgroundColor()
    }
}
```

整段删除（主题监听将在 Task 6 中由选中渲染修复重新接管）。

- [ ] **Step 3: 清理残留的"8pt"旧注释**

定位约 279-282 行：

```swift
// 任务 R1: 三栏布局 - 侧边栏贴边框，左右操作区圆角8pt 卡片，间距12pt
// 实现方式：mainSplitView 的 divider 提供间距（dividerStyle=.thin -> 1pt）
// 额外通过 sidebarView 和 paneContainer 的内边距增加视觉间距
// 操作区圆角8pt 由 createPaneContainer 中的 container.layer.cornerRadius = 8 实现
```

替换为：

```swift
// 任务 F2: 三栏布局 - 侧边栏贴边框，左右操作区撑满（仿访达），1pt 发丝线分隔（v0.6.5）
// mainSplitView 的 divider 提供 1pt 发丝线（dividerStyle=.thin）
```

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`，无 error。若出现 `applyPaneBackgroundColor` 相关的 "cannot find" 错误，检查是否漏删调用点。

- [ ] **Step 5: 提交检查点**

由于本项目非 git 仓库（交接文档第3节确认），跳过 git commit。改为记录变更日志到 `docs/MIGRATION_LOG.md` 追加一行：`- [v0.6.5-F2] 移除操作区圆角卡片，仿访达撑满`。

---

### Task 2: 重构双行工具栏布局（问题4+5）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/PaneToolbar.swift:30-354`（接受 BreadcrumbBar 嵌入）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:397-509`（createPaneContainer 内布局重构）

**Interfaces:**
- Consumes: Task 1 的无圆角 paneContainer
- Produces: PaneToolbar 提供 `setBreadcrumbBar(_:)` 公共方法接收 BreadcrumbBar；布局为 Row1（导航+刷新+面包屑）+ Row2（搜索/排序/分组/视图/工具）

- [ ] **Step 1: 在 PaneToolbar 添加 BreadcrumbBar 持有与公共方法**

打开 `UI/PaneToolbar.swift`。在 class `PaneToolbar` 的属性区（约 33-49 行，`toolsButton` 声明后）添加：

```swift
// 任务 F3: BreadcrumbBar 嵌入 Row1（刷新按钮后），紧贴刷新按钮
private var breadcrumbBar: BreadcrumbBar?
```

在 `setupRow1()` 方法（约 87-108 行）的 `NSLayoutConstraint.activate([...])` 之后添加公共方法：

```swift
// MARK: - 任务 F3: BreadcrumbBar 嵌入

/// 接收外部 BreadcrumbBar 并嵌入 Row1（刷新按钮之后，紧贴刷新按钮 4pt）
/// 由 MainWindowController.createPaneContainer 调用
func setBreadcrumbBar(_ bar: BreadcrumbBar) {
    breadcrumbBar = bar
    bar.translatesAutoresizingMaskIntoConstraints = false
    // 插入到 row1 的 refreshButton 之后（index 4）
    row1.insertArrangedSubview(bar, at: 4)
    // 面包屑 flex 撑满 Row1 剩余空间
    bar.setContentHuggingPriority(.defaultLow, for: .horizontal)
    bar.setContentCompressionResistancePriority(.defaultLow, for: .horizontal)
}
```

- [ ] **Step 2: 调整 PaneToolbar setupUI 高度约束说明**

`setupUI()` 中 `heightAnchor.constraint(equalToConstant: 72).isActive = true`（约 79 行）保持不变（Row1 32 + 间距 4 + Row2 32 + 边距 4 = 72）。无需修改，仅确认存在。

- [ ] **Step 3: 重构 createPaneContainer 布局**

打开 `UI/MainWindowController.swift`，定位 `createPaneContainer`（约 379-509 行）。将工具栏与面包屑的布局部分（约 397-488 行）替换。

替换前（约 397-488 行的关键约束块）：

```swift
// 面包屑导航栏
let breadcrumbBar = BreadcrumbBar()
breadcrumbBar.delegate = self
breadcrumbBar.translatesAutoresizingMaskIntoConstraints = false

// 工具栏
let toolbar = PaneToolbar()
toolbar.delegate = self
toolbar.translatesAutoresizingMaskIntoConstraints = false
...
// 添加到容器
container.addSubview(breadcrumbBar)
container.addSubview(toolbar)
...
// 任务 B1: 路径栏与 PaneToolbar 同行分区布局
let toolbarWidthConstraint = toolbar.widthAnchor.constraint(equalTo: container.widthAnchor, multiplier: 0.33)
let breadcrumbWidthConstraint = breadcrumbBar.widthAnchor.constraint(equalTo: container.widthAnchor, multiplier: 0.67)
toolbarWidthConstraint.priority = .defaultHigh
breadcrumbWidthConstraint.priority = .defaultHigh

NSLayoutConstraint.activate([
    accentBar.topAnchor.constraint(equalTo: container.topAnchor),
    accentBar.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    accentBar.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    accentBar.heightAnchor.constraint(equalToConstant: 2),

    // 任务 B1: toolbar 与 breadcrumbBar 同行（顶部 36pt）
    toolbar.topAnchor.constraint(equalTo: container.topAnchor),
    toolbar.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    toolbarWidthConstraint,
    toolbar.heightAnchor.constraint(equalToConstant: 36),

    breadcrumbBar.topAnchor.constraint(equalTo: container.topAnchor),
    breadcrumbBar.leadingAnchor.constraint(equalTo: toolbar.trailingAnchor),
    breadcrumbBar.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    breadcrumbWidthConstraint,
    breadcrumbBar.heightAnchor.constraint(equalToConstant: 36),

    listView.topAnchor.constraint(equalTo: toolbar.bottomAnchor),
    ...
])
```

替换为：

```swift
// 任务 F3: 双行工具栏布局（v0.6.5）
// Row1（32pt）= 后退/前进/上一级/刷新 + BreadcrumbBar（紧贴刷新）
// Row2（32pt）= 搜索/排序/分组/视图/工具
// PaneToolbar 总高 72pt

// 工具栏（内含双行 + BreadcrumbBar）
let toolbar = PaneToolbar()
toolbar.delegate = self
toolbar.translatesAutoresizingMaskIntoConstraints = false

// 面包屑导航栏（嵌入 PaneToolbar Row1）
let breadcrumbBar = BreadcrumbBar()
breadcrumbBar.delegate = self
breadcrumbBar.translatesAutoresizingMaskIntoConstraints = false
toolbar.setBreadcrumbBar(breadcrumbBar)

// 添加到容器
container.addSubview(toolbar)
container.addSubview(listView)
container.addSubview(gridView)
container.addSubview(detailsBar)

NSLayoutConstraint.activate([
    // accentBar 贴顶部（任务 F2：无圆角，不再裁剪）
    accentBar.topAnchor.constraint(equalTo: container.topAnchor),
    accentBar.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    accentBar.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    accentBar.heightAnchor.constraint(equalToConstant: 2),

    // 任务 F3: toolbar 占顶部 72pt（双行）
    toolbar.topAnchor.constraint(equalTo: container.topAnchor),
    toolbar.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    toolbar.trailingAnchor.constraint(equalTo: container.trailingAnchor),

    listView.topAnchor.constraint(equalTo: toolbar.bottomAnchor),
    listView.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    listView.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    listView.bottomAnchor.constraint(equalTo: detailsBar.topAnchor),

    gridView.topAnchor.constraint(equalTo: toolbar.bottomAnchor),
    gridView.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    gridView.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    gridView.bottomAnchor.constraint(equalTo: detailsBar.topAnchor),

    detailsBar.leadingAnchor.constraint(equalTo: container.leadingAnchor),
    detailsBar.trailingAnchor.constraint(equalTo: container.trailingAnchor),
    detailsBar.bottomAnchor.constraint(equalTo: container.bottomAnchor),
])
```

注意：移除了 `container.addSubview(breadcrumbBar)`（已嵌入 toolbar），移除了 33%/67% 宽度约束和 36pt 高度约束。

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。若出现 "unused variable breadcrumbBar" 警告，确认 switch 块中仍保存引用（`leftBreadcrumbBar = breadcrumbBar` 等保持不变）。

- [ ] **Step 5: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F3] 重构双行工具栏布局，BreadcrumbBar 嵌入 Row1 紧贴刷新按钮`

---

## 第二层：组件级修复

### Task 3: 收藏夹全面仿访达（问题1）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift:113-180`（移除 FFGlassView 卡片，改 NSVisualEffectView）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift:328-342`（makeOutlineView 设 sourceList 选中）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift:715-730`（收藏夹图标改蓝色模板）

**Interfaces:**
- Consumes: 无
- Produces: 收藏夹为蓝色模板图标 + sourceList 选中 + sidebar 材质（无 FFGlassView 卡片）

- [ ] **Step 1: 收藏夹 MaskView 改为 NSVisualEffectView sidebar 材质**

打开 `UI/SidebarView.swift`。先找到 `favoritesMaskView` 的属性声明（在文件前部搜索 `favoritesMaskView`），将其类型从 `FFGlassView!` 改为 `NSVisualEffectView!`。

定位 `setupUI()` 中约 113-117 行：

```swift
// E18: 强磨砂玻璃 - material 使用 .sidebar（NSVisualEffectView.Material.sidebar）
// level=.panel, cornerRadius=10 保持不变
favoritesMaskView = FFGlassView(level: .panel, cornerRadius: 10, material: .sidebar)
favoritesMaskView.translatesAutoresizingMaskIntoConstraints = false
addSubview(favoritesMaskView)
```

替换为：

```swift
// 任务 F4: 收藏夹仿访达 - 直接使用 NSVisualEffectView(.sidebar) 材质（v0.6.5）
// 移除 FFGlassView 圆角卡片包裹，改为访达侧边栏标准材质
favoritesMaskView = NSVisualEffectView()
favoritesMaskView.material = .sidebar
favoritesMaskView.blendingMode = .behindWindow
favoritesMaskView.state = .active
favoritesMaskView.translatesAutoresizingMaskIntoConstraints = false
addSubview(favoritesMaskView)
```

对 `tagsMaskView` 和 `deviceMaskView` 做同样处理（约 119-125 行）。注意这三个属性的声明类型都要从 `FFGlassView!` 改为 `NSVisualEffectView!`。

- [ ] **Step 2: makeOutlineView 设 sourceList 选中样式**

定位 `makeOutlineView()` 方法（约 328-342 行）。在 `ov.indentationPerLevel = 0` 之后添加：

```swift
// 任务 F4: 收藏夹仿访达 - sourceList 选中样式（半透明蓝高亮）
ov.selectionHighlightStyle = .sourceList
```

- [ ] **Step 3: 收藏夹图标改蓝色模板 SF Symbols**

定位收藏夹 cell 配置代码（约 715-730 行，搜索 `NSWorkspace.shared.icon(forFile: fav.path)`）。将：

```swift
let workspaceIcon = NSWorkspace.shared.icon(forFile: fav.path)
workspaceIcon.size = NSSize(width: 16, height: 16)
imageView.image = workspaceIcon
```

替换为：

```swift
// 任务 F4: 收藏夹仿访达 - 蓝色模板图标（访达风格）
let symbolName = Self.favoriteSymbolName(for: fav.path)
let icon = NSImage(systemSymbolName: symbolName, accessibilityDescription: fav.name)
icon?.isTemplate = true
icon?.size = NSSize(width: 16, height: 16)
imageView.image = icon
imageView.contentTintColor = NSColor.controlAccentColor  // 访达蓝色
```

并在 `FavoritesSidebarDataSource` 或 `SidebarView` 中添加辅助方法：

```swift
/// 根据收藏夹路径返回对应的 SF Symbol 名称（访达风格蓝色模板图标）
private static func favoriteSymbolName(for path: String) -> String {
    let home = FileManager.default.homeDirectoryForCurrentUser.path
    switch path {
    case (home as NSString).appendingPathComponent("Desktop"): return "desktopcomputer"
    case (home as NSString).appendingPathComponent("Documents"): return "folder.fill"
    case (home as NSString).appendingPathComponent("Downloads"): return "tray.and.arrow.down"
    case "/Applications": return "app"
    case home: return "house"
    default:
        // 用户自定义收藏：目录用 folder，文件用 doc
        var isDir: ObjCBool = false
        if FileManager.default.fileExists(atPath: path, isDirectory: &isDir), isDir.boolValue {
            return "folder"
        }
        return "doc"
    }
}
```

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。若 `favoritesMaskView` 报类型错误，检查所有引用点是否兼容 NSVisualEffectView（FFGlassView 是 NSVisualEffectView 子类，反向替换需确认无 FFGlassView 专属方法调用）。

- [ ] **Step 5: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F4] 收藏夹全面仿访达：蓝色模板图标 + sourceList 选中 + sidebar 材质`

---

### Task 4: 标签药丸裁切修复（问题2）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:579-639`（configureInlineTagPills）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:646-677`（makeTagPill 背景色）

**Interfaces:**
- Consumes: 无
- Produces: 药丸紧贴文件名后 8pt，长文件名截断不挤药丸，药丸背景带标签色

- [ ] **Step 1: 药丸容器 leading 改硬约束 + 降低 hugging**

打开 `UI/FileListView.swift`，定位 `configureInlineTagPills`（约 579-639 行）。在首次创建容器的 `NSLayoutConstraint.activate([...])` 块（约 610-614 行）：

```swift
NSLayoutConstraint.activate([
    stack.leadingAnchor.constraint(greaterThanOrEqualTo: textField.trailingAnchor, constant: 8),
    stack.trailingAnchor.constraint(equalTo: cellView.trailingAnchor, constant: -4),
    stack.centerYAnchor.constraint(equalTo: cellView.centerYAnchor),
])
```

替换为：

```swift
NSLayoutConstraint.activate([
    // 任务 F5: 药丸紧贴文件名后 8pt（硬约束，避免被推向右边缘裁切）
    stack.leadingAnchor.constraint(equalTo: textField.trailingAnchor, constant: 8),
    stack.trailingAnchor.constraint(lessThanOrEqualTo: cellView.trailingAnchor, constant: -4),
    stack.centerYAnchor.constraint(equalTo: cellView.centerYAnchor),
])
```

并在 `stack.setHuggingPriority(.defaultHigh, for: .horizontal)`（约 608 行）后改为：

```swift
// 任务 F5: 降低 hugging 让药丸不被推向右边缘
stack.setHuggingPriority(.defaultLow, for: .horizontal)
stack.setContentCompressionResistancePriority(.defaultHigh, for: .horizontal)
```

- [ ] **Step 2: 文件名设置截断不换行**

定位名称列 textField 配置（约 766-776 行，`if cellView.textField == nil` 块内）。在 `tf.lineBreakMode = .byTruncatingTail` 之后添加：

```swift
// 任务 F5: 文件名截断不换行，不挤压药丸
tf.cell?.truncatesLastVisibleLine = true
tf.cell?.wraps = false
```

- [ ] **Step 3: 药丸背景色改带标签色**

定位 `makeTagPill`（约 646-677 行）。将：

```swift
pill.layer?.backgroundColor = NSColor.controlBackgroundColor.cgColor
```

替换为：

```swift
// 任务 F5: 药丸背景带标签色浅色，提高对比度
let tagColor = NSColor(hex: tag.color) ?? .systemBlue
pill.layer?.backgroundColor = tagColor.withAlphaComponent(0.15).cgColor
```

对 `makeCountPill`（约 680-699 行）的 `pill.layer?.backgroundColor = NSColor.controlBackgroundColor.cgColor` 改为：

```swift
pill.layer?.backgroundColor = NSColor.secondarySystemFill.cgColor
```

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 5: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F5] 标签药丸裁切修复：硬约束 + 文件名截断 + 带色背景`

---

### Task 5: 应用图标圆角（问题7）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift:81-111`（brandIconView 加圆角）

**Interfaces:**
- Consumes: 无
- Produces: 侧边栏顶部应用图标 8pt 圆角矩形包裹

- [ ] **Step 1: brandIconView 加圆角与背景层**

打开 `UI/SidebarView.swift`，定位 `brandIconView` 配置（约 81-85 行）：

```swift
brandIconView = NSImageView()
brandIconView.image = NSImage(named: "AppIcon") ?? NSImage(systemSymbolName: "app", accessibilityDescription: nil)
brandIconView.imageScaling = .scaleProportionallyUpOrDown
brandIconView.translatesAutoresizingMaskIntoConstraints = false
brandView.addSubview(brandIconView)
```

替换为：

```swift
brandIconView = NSImageView()
brandIconView.image = NSImage(named: "AppIcon") ?? NSImage(systemSymbolName: "app", accessibilityDescription: nil)
brandIconView.imageScaling = .scaleProportionallyUpOrDown
brandIconView.translatesAutoresizingMaskIntoConstraints = false
// 任务 F6: 应用图标 8pt 圆角矩形包裹（v0.6.5）
brandIconView.wantsLayer = true
brandIconView.layer?.cornerRadius = 8
brandIconView.layer?.masksToBounds = true
brandIconView.layer?.backgroundColor = NSColor.controlBackgroundColor.cgColor
brandView.addSubview(brandIconView)
```

- [ ] **Step 2: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 3: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F6] 应用图标加 8pt 圆角矩形包裹`

---

### Task 6: 单击选中渲染 bug 修复（问题6）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:269-276`（mainContainer 改用 OpaqueContainerView）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:318-333`（主题监听改刷新选中）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:856-897`（tableViewSelectionDidChange 加 makeKey）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:195`（tableView 背景改半透明）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift`（viewDidLayout 设 appearance）

**Interfaces:**
- Consumes: Task 1 完成的无圆角布局
- Produces: 单击选中显示强调蓝+白字（获焦），失焦灰

采用 systematic-debugging 方法论：先验证根因 D（OpaqueContainerView 未使用），再依次验证 B（背景对比度）、A（key 状态）、C（appearance 时序）。

- [ ] **Step 1: 验证并修复根因 D - mainContainer 改用 OpaqueContainerView**

打开 `UI/MainWindowController.swift`。先确认 `OpaqueContainerView` 类定义存在（约 79-83 行，重写 `isOpaque = true`）。

定位 `let mainContainer = NSView()`（约 270 行），替换为：

```swift
// 任务 F7: 使用 OpaqueContainerView 修复鼠标穿透与选中渲染（v0.6.5）
let mainContainer = OpaqueContainerView()
```

- [ ] **Step 2: 修复根因 B - tableView 背景改半透明白**

打开 `UI/FileListView.swift`，定位约 195 行：

```swift
tableView.backgroundColor = .clear
```

替换为：

```swift
// 任务 F7: 半透明背景给选中色提供底色衬托（v0.6.5）
// 完全透明时标准蓝色在玻璃背景上对比度不足
tableView.backgroundColor = NSColor.textBackgroundColor.withAlphaComponent(0.6)
```

注意：`tableView.enclosingScrollView?.drawsBackground = false` 等保持不变（scrollView 仍透明，仅 tableView 有半透明底）。

- [ ] **Step 3: 修复根因 A - tableViewSelectionDidChange 加 makeKey**

定位 `tableViewSelectionDidChange`（约 866-897 行）。找到现有的 makeFirstResponder 块（约 876-879 行）：

```swift
// 确保 tableView 是 firstResponder，否则选中高亮会变成灰色（de-emphasized）
if let window = tableView.window, window.firstResponder !== tableView {
    window.makeFirstResponder(tableView)
}
```

替换为：

```swift
// 任务 F7: 确保 window 为 key + tableView 为 firstResponder（v0.6.5）
// 透明窗口的 key 状态不稳定，需显式 makeKey + makeFirstResponder
if let window = tableView.window {
    if !window.isKeyWindow {
        window.makeKeyAndOrderFront(nil)
    }
    if window.firstResponder !== tableView {
        window.makeFirstResponder(tableView)
    }
}
```

- [ ] **Step 4: 修复根因 C - viewDidLayout 设 appearance**

在 `FileListView` 类中找到 `viewDidLayout`（若无则添加 override）。添加或修改为：

```swift
// 任务 F7: 显式同步 appearance，确保选中色解析正确（v0.6.5）
public override func viewDidLayout() {
    super.viewDidLayout()
    tableView.appearance = NSApp.appearance
}
```

- [ ] **Step 5: 在 MainWindowController 重新添加主题监听（刷新选中）**

打开 `UI/MainWindowController.swift`，在 Task 1 Step 2 删除主题监听的位置（约 321 行原位置）重新添加：

```swift
// 任务 F7: 监听主题变更，刷新所有 FileListView 的 appearance（v0.6.5）
ThemeManager.shared.onModeChanged = { [weak self] _ in
    DispatchQueue.main.async {
        self?.leftFileListView?.viewDidLayout()
        self?.rightFileListView?.viewDidLayout()
    }
}
```

- [ ] **Step 6: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 7: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F7] 单击选中渲染修复：OpaqueContainerView + 半透明背景 + makeKey + appearance 同步`

---

### Task 7: 缩略图加载链修复（问题8）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/Bridge/ThumbnailManager.swift:43-107`（增加日志）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:791-827`（缩略图调用层修复）

**Interfaces:**
- Consumes: 无
- Produces: 图片文件显示真实内容缩略图

- [ ] **Step 1: ThumbnailManager 增加请求/成功/失败日志**

打开 `Bridge/ThumbnailManager.swift`，定位 `generateThumbnail`（约 43-107 行）。在"3. 异步生成"块之前（约 63 行）添加：

```swift
// 任务 F8: 调试日志（v0.6.5）
print("[Thumb] 请求生成: \(path) size=\(size)")
```

在 `generator.generateBestRepresentation` 的回调中，error 分支（约 85-88 行）添加：

```swift
print("[Thumb] 生成失败: \(path) - \(error.localizedDescription)")
```

成功分支（约 96-105 行，`let image = NSImage(...)` 之后）添加：

```swift
print("[Thumb] 生成成功: \(path) \(thumbnail.cgImage.width)x\(thumbnail.cgImage.height)")
```

- [ ] **Step 2: FileListView 缩略图调用层修复 - 取消旧请求 + 路径校验**

打开 `UI/FileListView.swift`，定位缩略图加载块（约 818-827 行）：

```swift
// 非目录文件额外异步加载 QuickLook 缩略图（图片、视频等预览）
if !entry.isDirectory {
    let path = entry.path
    ThumbnailManager.shared.generateThumbnail(path: path, size: CGSize(width: 32, height: 32)) { [weak cellView] image in
        guard let image = image else { return }
        if cellView?.textField?.stringValue == entry.name {
            cellView?.imageView?.image = image
        }
    }
}
```

替换为：

```swift
// 任务 F8: 缩略图加载层修复（v0.6.5）
// 1) 取消该 cell 上一次的缩略图请求（避免旧请求覆盖新 cell）
// 2) 回调校验改为完整路径（避免同名文件误覆盖）
// 3) 先显示文件类型图标作为占位，缩略图返回后再替换
if !entry.isDirectory {
    let path = entry.path
    // 取消旧请求（用 cell 的 identifier 记录上一次路径）
    if let oldPath = cellView.identifier?.rawValue.components(separatedBy: "|").last,
       oldPath != path {
        ThumbnailManager.shared.cancelGeneration(for: oldPath)
    }
    // 更新 cell identifier 以记录当前路径（格式 "colName|path"）
    cellView.identifier = NSUserInterfaceItemIdentifier("\(cellID.rawValue)|\(path)")

    ThumbnailManager.shared.generateThumbnail(path: path, size: CGSize(width: 32, height: 32)) { [weak cellView] image in
        guard let image = image else { return }
        DispatchQueue.main.async {
            // 校验 cell 仍显示同一文件（用完整路径而非文件名）
            guard let cell = cellView,
                  let currentID = cell.identifier?.rawValue,
                  currentPath == path else { return }
            cell.imageView?.image = image
        }
    }
} else {
    // 目录不加载缩略图，确保 imageView 是文件夹图标
    let workspaceIcon = NSWorkspace.shared.icon(forFile: entry.path)
    workspaceIcon.size = NSSize(width: 18, height: 18)
    cellView.imageView?.image = workspaceIcon
}
```

注意：上述 `currentPath` 是闭包内未定义的变量，修正为从 `currentID` 解析：

```swift
// 校验 cell 仍显示同一文件（用完整路径而非文件名）
guard let cell = cellView,
      let currentID = cell.identifier?.rawValue,
      let currentPath = currentID.components(separatedBy: "|").last,
      currentPath == path else { return }
cell.imageView?.image = image
```

- [ ] **Step 3: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 4: 运行验证缩略图加载**

启动应用，进入含图片的目录，观察控制台输出：
- 应看到 `[Thumb] 请求生成: ...` 日志
- 应看到 `[Thumb] 生成成功: ...` 日志
- 文件列表中图片文件应显示真实内容缩略图（非空白）

若看到 `生成失败` 日志，记录错误信息供后续排查。

- [ ] **Step 5: 记录变更日志**

追加到 `docs/MIGRATION_LOG.md`：`- [v0.6.5-F8] 缩略图加载链修复：取消旧请求 + 路径校验 + 日志`

---

## 第三层：全量 UI 巡检

### Task 8: 全量 UI 巡检（代码级）

**Files:**
- Read: 所有 `UI/*.swift` 文件
- Create: `docs/superpowers/specs/2026-07-27-v0.6.5-ui-audit-report.md`

**Interfaces:**
- Consumes: Task 1-7 完成的修复
- Produces: 巡检问题清单（含 file:line 证据、根因初判、P0/P1/P2 优先级）

- [ ] **Step 1: 巡检主窗口列表视图 + 网格视图**

读取 `UI/FileListView.swift`、`UI/FileGridView.swift`、`UI/MainWindowController.swift`，对照访达标准检查：
- 列顺序、列宽、行高
- 选中样式、悬停效果
- 图标、文字颜色（隐藏/系统保护文件）
- 滚动条样式
- 网格视图的卡片布局、间距

记录问题到巡检报告草稿。

- [ ] **Step 2: 巡检侧边栏三 section + 路径栏交互**

读取 `UI/SidebarView.swift`、`UI/BreadcrumbBar.swift`，检查：
- 收藏夹/位置/标签三个 section 的 header 样式
- 设备模块布局（B8-B11 任务是否仍生效）
- 路径栏同级跳转、点击跳转交互
- 拖拽添加收藏夹

记录问题。

- [ ] **Step 3: 巡检设置窗口 + 查重窗口 + 搜索面板**

读取 `UI/SettingsWindowController.swift`、`UI/DuplicateScanWindowController.swift`、`UI/SearchPanelController.swift`，检查：
- 各设置面板（外观/通用/标签/高级）的视觉一致性
- 查重窗口扫描进度/分组列表/预览
- 搜索面板条件/结果

记录问题。

- [ ] **Step 4: 巡检其余窗口与对话框**

读取以下文件并检查：
- `UI/AboutWindowController.swift`（关于窗口）
- `UI/FileInfoWindowController.swift`（文件信息窗口）
- `UI/BatchRenameWindowController.swift`（批量重命名）
- `UI/DeleteConfirmDialog.swift`、`UI/ConnectServerDialog.swift`、`UI/TagSelectorDialog.swift`（对话框）
- `UI/ProgressDialog.swift`、`UI/TaskPanelWindowController.swift`（进度/任务）
- `UI/ExpandableDetailsBar.swift`（详情栏）
- 右键菜单（在各文件中搜索 `NSMenu`、`menuNeedsUpdate`）

记录问题。

- [ ] **Step 5: 巡检主题适配与中文化**

全局检查：
- 在每个文件中搜索 `.system` 主题引用（应为 light/dark 二态）
- 搜索英文 UI 文案（应为简体中文）
- 搜索 SF Symbols 图标完整性（按钮是否有图标）

记录问题。

- [ ] **Step 6: 汇总巡检报告**

将 Step 1-5 记录的问题汇总到 `docs/superpowers/specs/2026-07-27-v0.6.5-ui-audit-report.md`，格式：

```markdown
# v0.6.5 UI 巡检报告

## 巡检范围
（列出 14 个巡检项）

## 问题清单

### P0（本轮修复）
- [ ] [窗口名] 问题描述 - `file:line` - 根因初判

### P1（下一轮修复）
- [ ] ...

### P2（长期优化）
- [ ] ...

## 统计
- P0: X 项
- P1: Y 项
- P2: Z 项
```

- [ ] **Step 7: 与用户确认巡检结果**

将巡检报告呈现给用户，确认 P0 问题清单纳入本轮修复，P1/P2 纳入下一轮。等待用户确认后，P0 问题作为追加任务纳入本计划。

---

### Task 9: P0 巡检问题修复（待 Task 8 完成后填充）

**说明：** 本任务的具体步骤取决于 Task 8 巡检发现的 P0 问题。Task 8 完成并与用户确认后，将 P0 问题逐项展开为独立的子任务（每个子任务遵循 TDD + 构建验证 + 日志记录的步骤结构）。

**Files:**
- 待定（取决于巡检结果）

**Interfaces:**
- Consumes: Task 8 的 P0 问题清单
- Produces: P0 问题全部修复

- [ ] **Step 1: 待 Task 8 完成后展开**

---

## 收尾

### Task 10: 整体验收构建与日志

**Files:**
- Modify: `docs/MIGRATION_LOG.md`（追加 v0.6.5 总结）

- [ ] **Step 1: Debug 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|warning:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`，无 error。warning 可接受但需记录。

- [ ] **Step 2: Release 构建验证**

```bash
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Release build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 3: 追加 v0.6.5 变更总结到 MIGRATION_LOG.md**

在 `docs/MIGRATION_LOG.md` 顶部追加：

```markdown
## v0.6.5（2026-07-27）仿访达 UI 重构

本轮修复用户反馈的 8 个 UI 问题 + 全量 UI 巡检：

- [F2] 移除操作区圆角卡片，仿访达撑满
- [F3] 重构双行工具栏布局，BreadcrumbBar 嵌入 Row1 紧贴刷新按钮
- [F4] 收藏夹全面仿访达：蓝色模板图标 + sourceList 选中 + sidebar 材质
- [F5] 标签药丸裁切修复：硬约束 + 文件名截断 + 带色背景
- [F6] 应用图标加 8pt 圆角矩形包裹
- [F7] 单击选中渲染修复：OpaqueContainerView + 半透明背景 + makeKey + appearance 同步
- [F8] 缩略图加载链修复：取消旧请求 + 路径校验 + 日志
- [F9] 全量 UI 巡检（详见 2026-07-27-v0.6.5-ui-audit-report.md）
```

- [ ] **Step 4: 呈交用户截图验收**

向用户报告：
- 8 个问题修复完成
- Debug + Release 构建通过
- 巡检报告已产出（P0/P1/P2 分级）
- 请用户截图验收，对照设计稿检查修复效果

等待用户截图反馈。如有偏差，进入下一轮修复。

# v0.6.6 仿访达完整重设计 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v0.6.5 用户反馈的 20 个问题，完全克隆访达视觉与交互。

**Architecture:** 六层分阶段推进：窗口架构重建 -> 侧边栏重构 -> 文件列表重构 -> 网格视图重构 -> 功能接线修复 -> 视觉打磨。纯 UI 层修改，不动 Rust Core。

**Tech Stack:** Swift 5.9 + AppKit（NSTableView/NSOutlineView/NSCollectionView/NSStackView/NSSplitView）、NSVisualEffectView、QLThumbnailGenerator、SF Symbols。Xcode-beta 构建。

## Global Constraints

- 必须使用 Xcode-beta 构建：`export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer`
- 项目路径：`/Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native`
- 构建：`xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj -scheme FlowFinderNative -configuration Debug build`
- 部署目标 macOS 12.0（MACOSX_DEPLOYMENT_TARGET = 12.0）
- 所有 UI 文案和代码注释使用简体中文
- 主题切换保持 light/dark 二态（无 system 自动跟随）
- 完全克隆访达视觉
- 每个修复点标注任务编号（F10- 系列，延续 F9）
- 无单元测试框架，验收靠"构建通过 + 截图"
- git 仓库，当前分支 main，BASE commit 35dd1f7

---

## 文件结构

### 将要修改的文件

| 文件 | 职责 | 本轮改动 |
|------|------|---------|
| `UI/MainWindowController.swift` | 主窗口 | 移除 titlebarView 占位、window.delegate、设备浮层、功能接线 |
| `UI/SidebarView.swift` | 侧边栏 | 收藏夹彩色图标、标签垂直布局、设备移出 |
| `UI/FileListView.swift` | 文件列表 | 选中背景修复、列宽自动、药丸列头、分组 section、列头排序、缩略图、在另一侧打开 |
| `UI/FileGridView.swift` | 网格视图 | 选中样式、缩略图复用、appearance 同步、分组 section |
| `UI/PaneToolbar.swift` | 操作区工具栏 | 移除升降序按钮、搜索实时 |
| `Model/PaneState.swift` | 面板状态 | 分组聚合 groupedFiles、搜索基于原始 files |
| `Bridge/ThumbnailManager.swift` | 缩略图管理 | NSImage.size 修复、representationTypes |
| `Model/FileEntry.swift` | 文件条目 | 可能增加分类辅助方法（种类分组用） |

---

## 第一层：窗口架构重建

### Task 1: 移除 title 栏占位 + window.delegate 设置（问题9/15-辅助）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift:215-303`（setupUI 窗口配置区）

**Interfaces:**
- Consumes: 无
- Produces: 窗口无 titlebarView 占位，mainSplitView 顶到顶部，红绿灯浮在侧边栏上方

- [ ] **Step 1: 移除 titlebarView 占位，mainSplitView 顶到顶部**

打开 `UI/MainWindowController.swift`，定位 setupUI 中 titlebarView 创建与约束（约 267-303 行）。

`titlebarAppearsTransparent = true` 和 `titleVisibility = .hidden` 已设置（226-227 行），保留。

移除 titlebarView 创建（约 268-271 行）：
```swift
// 任务 R2: titlebar 仅保留红绿灯（系统提供），移除中间的应用图标+名称
// 应用图标+名称已移到侧边栏顶部（任务 R3，由 SidebarView 内部处理）
```
和
```swift
// 任务 R2: titlebar 仅保留红绿灯空间（28pt 高，透明背景）
let titlebarView = NSView()
titlebarView.translatesAutoresizingMaskIntoConstraints = false
titlebarView.wantsLayer = true
titlebarView.layer?.backgroundColor = NSColor.clear.cgColor
```

移除 `mainContainer.addSubview(titlebarView)`（约 279 行）。

修改约束块（约 286-303 行），将 mainSplitView 顶到 mainContainer 顶部：
```swift
// 任务 F10-1: 移除 titlebarView 占位，mainSplitView 顶到顶部（v0.6.6）
// 红绿灯由系统自动浮在侧边栏顶部上方（titlebarAppearsTransparent=true）
// 侧边栏内部顶部留出红绿灯安全区（由 SidebarView 处理）
NSLayoutConstraint.activate([
    // mainSplitView 顶到 mainContainer 顶部（红绿灯浮在上方）
    mainSplitView.topAnchor.constraint(equalTo: mainContainer.topAnchor),
    mainSplitView.leadingAnchor.constraint(equalTo: mainContainer.leadingAnchor),
    mainSplitView.trailingAnchor.constraint(equalTo: mainContainer.trailingAnchor),
    mainSplitView.bottomAnchor.constraint(equalTo: mainContainer.bottomAnchor),

    taskProgressBar.leadingAnchor.constraint(equalTo: mainContainer.leadingAnchor),
    taskProgressBar.trailingAnchor.constraint(equalTo: mainContainer.trailingAnchor),
    taskProgressBar.bottomAnchor.constraint(equalTo: mainContainer.bottomAnchor),
    taskProgressBar.heightAnchor.constraint(equalToConstant: 0),
])
```

- [ ] **Step 2: 设置 window.delegate = self**

在 setupUI 中（window 配置区，约 228 行后）添加：
```swift
// 任务 F10-1: 设置 window.delegate，使 windowDidBecomeKey 等回调生效（v0.6.6）
// 修复问题15/16 右键菜单不生效的潜在根因
window.delegate = self
```

确认 MainWindowController 已声明 NSWindowDelegate 协议（grep `NSWindowDelegate` 确认，若无则添加）。

- [ ] **Step 3: SidebarView 顶部红绿灯安全区调整**

`SidebarView.swift` 的 brandView 当前 `topAnchor.constraint(equalTo: topAnchor, constant: 32)`（约 99 行）。32pt 是为红绿灯留的安全区，保留。确认侧边栏内容从红绿灯下方开始。

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。若 titlebarView 有残留引用导致编译错误，搜索 `titlebarView` 全文清除。

- [ ] **Step 5: Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift
git commit -m "feat: [F10-1] 移除title栏占位+window.delegate设置（问题9/15辅助）"
```

---

### Task 2: 单击选中渲染修复 - 移除错误背景（问题14）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:202`（tableView.backgroundColor）

**Interfaces:**
- Consumes: Task 1 的 window.delegate 设置
- Produces: 单击选中显示强调蓝高亮

- [ ] **Step 1: tableView.backgroundColor 改回 .clear**

打开 `UI/FileListView.swift`，定位约 202 行：
```swift
// 任务 F7: 半透明背景给选中色提供底色衬托（v0.6.5）
// 完全透明时标准蓝色在玻璃背景上对比度不足
tableView.backgroundColor = NSColor.textBackgroundColor.withAlphaComponent(0.6)
```

替换为：
```swift
// 任务 F10-2: 修正 F7 错误 - tableView 背景改回 .clear（v0.6.6）
// F7 的 0.6 alpha 背景恰恰遮挡了 NSTableRowView.drawSelection 的选中色
// 选中色由 NSVisualEffectView 玻璃材质衬托，无需 tableView 自身底色
tableView.backgroundColor = .clear
```

- [ ] **Step 2: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 3: Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/FileListView.swift
git commit -m "fix: [F10-2] 选中渲染修复-移除F7错误的0.6alpha背景（问题14）"
```

---

### Task 3: 设备区域浮动到窗口左下角独立区域（问题18）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift`（移除设备区域代码）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift`（新增设备浮层）

**Interfaces:**
- Consumes: Task 1 的窗口架构
- Produces: 设备区域浮动在窗口左下角，展开显示所有设备不需滚动

- [ ] **Step 1: 从 SidebarView 移除设备区域**

打开 `UI/SidebarView.swift`，搜索设备相关代码（`deviceMaskView`、`deviceHeaderView`、`deviceOutlineView`、`DeviceHeaderView`）。

将设备区域的视图创建、约束、dataSource/delegate 设置代码从 SidebarView 移除（保留设备数据获取逻辑，迁移到新浮层）。注意保留 `DeviceHeaderView` 类定义（移到新文件或留在 SidebarView 作为公共类）。

设备数据获取逻辑（如 statfs 读取磁盘容量）迁移到 MainWindowController 或独立 DevicePanelController。

- [ ] **Step 2: 在 MainWindowController 新建设备浮层**

在 `MainWindowController.swift` 中新增设备浮层视图。浮层定位：
```swift
// 任务 F10-3: 设备区域浮动在窗口左下角独立区域（v0.6.6）
// 与侧边栏收藏夹/标签模块视觉分离
private var devicePanel: NSView!

// 在 setupUI 中创建设备浮层
devicePanel = createDevicePanel()
mainContainer.addSubview(devicePanel)
NSLayoutConstraint.activate([
    devicePanel.leadingAnchor.constraint(equalTo: mainContainer.leadingAnchor, constant: 8),
    devicePanel.bottomAnchor.constraint(equalTo: mainContainer.bottomAnchor, constant: -8),
    devicePanel.widthAnchor.constraint(equalToConstant: 200),
])
```

- [ ] **Step 3: 实现 createDevicePanel 方法**

```swift
/// 任务 F10-3: 创建设备浮层（浮动在窗口左下角，展开显示所有设备不需滚动）
private func createDevicePanel() -> NSView {
    let panel = NSView()
    panel.translatesAutoresizingMaskIntoConstraints = false
    panel.wantsLayer = true
    panel.layer?.backgroundColor = NSColor.controlBackgroundColor.withAlphaComponent(0.8)
    panel.layer?.cornerRadius = 8
    panel.layer?.masksToBounds = true

    // 设备列表（纵向 stack，根据设备数量自适应高度，不需滚动条）
    let stack = NSStackView()
    stack.orientation = .vertical
    stack.spacing = 4
    stack.translatesAutoresizingMaskIntoConstraints = false
    panel.addSubview(stack)
    NSLayoutConstraint.activate([
        stack.topAnchor.constraint(equalTo: panel.topAnchor, constant: 8),
        stack.leadingAnchor.constraint(equalTo: panel.leadingAnchor, constant: 8),
        stack.trailingAnchor.constraint(equalTo: panel.trailingAnchor, constant: -8),
        stack.bottomAnchor.constraint(equalTo: panel.bottomAnchor, constant: -8),
    ])

    // TODO: 填充设备列表（迁移 SidebarView 的设备数据获取逻辑）
    // 每个设备行：图标 + 名称 + 容量进度条

    return panel
}
```

注意：上述 TODO 需替换为实际设备填充逻辑。迁移 SidebarView 的设备数据获取（statfs 读取、过滤隐藏卷）和行视图构建。

- [ ] **Step 4: 构建验证**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`。若有 SidebarView 残留引用，清除。

- [ ] **Step 5: Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/SidebarView.swift FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift
git commit -m "feat: [F10-3] 设备区域浮动到窗口左下角独立区域（问题18）"
```

---

## 第二层：侧边栏重构

### Task 4: 收藏夹彩色真实图标 + 不折叠 + 标签在下方（问题1）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift`（收藏夹图标、布局）

**Interfaces:**
- Consumes: Task 3（设备区域已移出）
- Produces: 收藏夹彩色真实图标、不折叠、左对齐、标签在下方

- [ ] **Step 1: 收藏夹图标改回彩色真实图标（修正 F4 错误）**

打开 `UI/SidebarView.swift`，搜索收藏夹图标配置（F4 改为 SF Symbols 蓝色模板的位置，搜索 `favoriteSymbolName` 或 `isTemplate`）。

将 F4 的蓝色模板 SF Symbols 改回彩色真实图标：
```swift
// 任务 F10-4: 修正 F4 错误 - 收藏夹改回彩色真实图标（v0.6.6）
// F4 错误地改为蓝色模板 SF Symbols，应使用 NSWorkspace 真实位置图标
let workspaceIcon = NSWorkspace.shared.icon(forFile: fav.path)
workspaceIcon.size = NSSize(width: 20, height: 20)  // 放大对齐访达（原16pt改20pt）
imageView.image = workspaceIcon
imageView.contentTintColor = nil  // 移除 F4 的 controlAccentColor
```

移除 `favoriteSymbolName(for:)` 辅助方法（F4 新增的，不再需要）。

- [ ] **Step 2: 确认收藏夹不折叠 + 左对齐**

确认 `indentationPerLevel = 0`（F1 设置，保留）。确认收藏夹默认全部展开（无折叠箭头）。若 outlineView 有折叠功能，设 `outlineView.allowsCollapseOfRoot = false` 或在 delegate 中阻止折叠。

- [ ] **Step 3: 确认标签模块在收藏夹下方**

确认侧边栏纵向布局：brandView -> 收藏夹 section（动态高度）-> 标签 section（紧跟下方）。设备区域已移出（Task 3）。标签 section 的 `topAnchor` 应 `equalTo favoritesSection.bottomAnchor`。

- [ ] **Step 4: 构建验证 + Commit**

构建验证通过后：
```bash
git add FlowFinderNative/FlowFinderNative/UI/SidebarView.swift
git commit -m "fix: [F10-4] 收藏夹彩色真实图标+不折叠+标签在下方（问题1，修正F4）"
```

---

### Task 5: 标签模块重设计 - 圆点+文字垂直排列（问题2）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/SidebarView.swift`（TagFlowView 重构）

**Interfaces:**
- Consumes: Task 4
- Produces: 标签彩色圆点+完整标签名垂直排列，不遮挡标题

- [ ] **Step 1: TagFlowView 改为垂直纵向布局**

打开 `UI/SidebarView.swift`，搜索 `TagFlowView`。当前是横向 wrap 布局，改为垂直纵向排列。

将 TagFlowView 的布局方向从横向改为纵向：
```swift
// 任务 F10-5: 标签模块改为垂直纵向排列（v0.6.6）
// 每行一个标签：彩色圆点 + 完整标签名
// 行高 28pt 对齐访达侧边栏项目高度
```

- [ ] **Step 2: 标签行视图：圆点 + 完整文字**

每个标签行视图：
```swift
/// 任务 F10-5: 标签行视图（圆点 + 完整标签名，垂直排列）
private func makeTagRow(tag: Tag) -> NSView {
    let row = NSView()
    row.translatesAutoresizingMaskIntoConstraints = false

    let dot = NSView()
    dot.wantsLayer = true
    dot.layer?.backgroundColor = (NSColor(hex: tag.color) ?? .systemBlue).cgColor
    dot.layer?.cornerRadius = 4
    dot.translatesAutoresizingMaskIntoConstraints = false

    let label = NSTextField(labelWithString: tag.name)
    label.font = NSFont.systemFont(ofSize: 13)
    label.lineBreakMode = .byTruncatingTail
    label.translatesAutoresizingMaskIntoConstraints = false

    row.addSubview(dot)
    row.addSubview(label)

    NSLayoutConstraint.activate([
        row.heightAnchor.constraint(equalToConstant: 28),
        dot.leadingAnchor.constraint(equalTo: row.leadingAnchor, constant: 8),
        dot.centerYAnchor.constraint(equalTo: row.centerYAnchor),
        dot.widthAnchor.constraint(equalToConstant: 8),
        dot.heightAnchor.constraint(equalToConstant: 8),
        label.leadingAnchor.constraint(equalTo: dot.trailingAnchor, constant: 8),
        label.trailingAnchor.constraint(equalTo: row.trailingAnchor, constant: -8),
        label.centerYAnchor.constraint(equalTo: row.centerYAnchor),
    ])
    return row
}
```

- [ ] **Step 3: 标签 section header 不被遮挡**

确认标签 section header（"标签"小标题）与标签列表之间留足间距。header 固定在 section 顶部，标签列表在下方。section 整体 `topAnchor = favoritesSection.bottomAnchor`。

- [ ] **Step 4: 构建验证 + Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/SidebarView.swift
git commit -m "feat: [F10-5] 标签模块重设计-圆点+文字垂直排列（问题2）"
```

---

## 第三层：文件列表重构

### Task 6: 列宽自动调整占满操作区（问题4）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift:214-248`（列定义）

- [ ] **Step 1: 四列 resizingMask 添加 .autoresizingMask**

打开 `UI/FileListView.swift`，定位四列定义（约 214-248 行）。每列的 `resizingMask` 从 `.userResizingMask` 改为 `[.userResizingMask, .autoresizingMask]`。

- [ ] **Step 2: 设置 columnAutoresizingStyle**

在 tableView 配置区（约 190 行附近）添加：
```swift
// 任务 F10-6: 列宽自动调整占满操作区（v0.6.6）
// 最后一列自动填充剩余宽度
tableView.columnAutoresizingStyle = .lastColumnOnly
```

- [ ] **Step 3: 移除过时的 sizeToFit 注释**

搜索 `sizeToFit` 相关注释（约 267 行），移除基于"第5列"的过时注释。

- [ ] **Step 4: 构建验证 + Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/FileListView.swift
git commit -m "feat: [F10-6] 列宽自动调整占满操作区（问题4）"
```

---

### Task 7: 药丸列头 + 可拖动调整顺序 + 列头点击升降序（问题5/12）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift`（列头自定义）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/PaneToolbar.swift`（移除升降序按钮）

- [ ] **Step 1: 移除 PaneToolbar 的升降序按钮**

打开 `UI/PaneToolbar.swift`，移除 `sortDirectionButton` 的创建（约 162-168 行）、Row2 布局中的引用（约 194 行）、`sortDirectionToggled` 方法（约 278-284 行）。

修改 `sortSelected` 不再读取 sortDirectionButton 状态。

- [ ] **Step 2: FileListView 自定义药丸列头**

在 `UI/FileListView.swift` 中实现自定义列头。为每列设置自定义 headerCell 或 headerView：
```swift
// 任务 F10-7: 访达风格药丸列头（圆角背景+文字+排序箭头，可点击排序）
// 列头点击循环升/降序，显示当前排序状态箭头
```

实现列头点击排序逻辑：点击列头 -> 切换该列升/降序 -> 更新箭头图标。

- [ ] **Step 3: 启用列拖动重排**

```swift
// 任务 F10-7: 启用列拖动调整顺序
tableView.allowsColumnReordering = true
```

- [ ] **Step 4: 构建验证 + Commit**

```bash
git add FlowFinderNative/FlowFinderNative/UI/FileListView.swift FlowFinderNative/FlowFinderNative/UI/PaneToolbar.swift
git commit -m "feat: [F10-7] 药丸列头+可拖动+列头点击升降序（问题5/12）"
```

---

### Task 8: 分组功能照搬访达（问题6）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/Model/PaneState.swift`（分组聚合）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift`（section 渲染）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileGridView.swift`（section 渲染）

- [ ] **Step 1: PaneState 新增 groupedFiles 计算属性**

打开 `Model/PaneState.swift`，新增分组聚合：
```swift
// 任务 F10-8: 分组聚合（v0.6.6）
// 按种类/日期/大小聚合文件
var groupedFiles: [(key: String, entries: [FileEntry])] {
    guard state.groupBy != "none" else {
        return [(key: "全部", entries: state.files)]
    }
    switch state.groupBy {
    case "kind": return groupByKind()
    case "date": return groupByDate()
    case "size": return groupBySize()
    default: return [(key: "全部", entries: state.files)]
    }
}
```

实现 `groupByKind()`/`groupByDate()`/`groupBySize()` 方法，按 spec 第6.4节的分类规则聚合。

- [ ] **Step 2: FileListView 实现 sectioned 渲染**

`FileListView.swift` 改为 sectioned 渲染。每个 group 一个 section header（显示 group key + 数量，如"图片 (12)"）。

注意：NSTableView 的 sectioned 渲染需要用 group 行（特殊 row）或自定义 section header view。参考访达列表视图的分组样式（section header 浅灰背景、粘性定位）。

- [ ] **Step 3: FileGridView 实现 sectioned 渲染**

`FileGridView.swift` 同样实现 sectioned 渲染（NSCollectionView 多 section）。

- [ ] **Step 4: 构建验证 + Commit**

```bash
git add FlowFinderNative/FlowFinderNative/Model/PaneState.swift FlowFinderNative/FlowFinderNative/UI/FileListView.swift FlowFinderNative/FlowFinderNative/UI/FileGridView.swift
git commit -m "feat: [F10-8] 分组功能照搬访达-按种类/日期/大小（问题6）"
```

---

## 第四层：网格视图重构

### Task 9: 网格视图照搬访达 + 缩略图修复（问题7/10）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileGridView.swift`（选中样式、缩略图复用、appearance）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/Bridge/ThumbnailManager.swift`（NSImage.size、representationTypes）

- [ ] **Step 1: ThumbnailManager NSImage.size 修复**

打开 `Bridge/ThumbnailManager.swift`，定位约 99-102 行：
```swift
let image = NSImage(
    cgImage: thumbnail.cgImage,
    size: CGSize(width: thumbnail.cgImage.width, height: thumbnail.cgImage.height)
)
```

替换为：
```swift
// 任务 F10-9: 修正 NSImage.size 用请求的 point size（v0.6.6）
// 原代码用 cgImage 像素尺寸，Retina 下尺寸翻倍导致模糊
let image = NSImage(
    cgImage: thumbnail.cgImage,
    size: size  // 用请求的 point size，非 cgImage 像素尺寸
)
```

- [ ] **Step 2: representationTypes 改为 .all**

定位约 72 行：
```swift
representationTypes: .thumbnail
```

替换为：
```swift
// 任务 F10-9: representationTypes 改为 .all，无缩略图文件类型回退到图标
representationTypes: .all
```

- [ ] **Step 3: FileGridView 选中样式改为访达实心蓝**

打开 `UI/FileGridView.swift`，定位约 84-92 行 `isSelected` didSet：
```swift
view.layer?.backgroundColor = isSelected
    ? NSColor.controlAccentColor.withAlphaComponent(0.15).cgColor
    : NSColor.clear.cgColor
view.layer?.cornerRadius = isSelected ? 6 : 0
```

替换为：
```swift
// 任务 F10-9: 访达风格实心蓝半透明选中（v0.6.6）
view.layer?.backgroundColor = isSelected
    ? NSColor.controlAccentColor.withAlphaComponent(0.25).cgColor
    : NSColor.clear.cgColor
view.layer?.cornerRadius = isSelected ? 8 : 0
```

- [ ] **Step 4: FileGridView 缩略图复用校验（F8 遗漏修复）**

FileGridCollectionViewItem 增加 `currentPath` 属性 + `prepareForReuse()` 取消旧请求 + 回调路径校验（参照 FileListView F8 修复）。

- [ ] **Step 5: FileGridView appearance 同步（F7 遗漏修复）**

FileGridView 增加 `layout()` override + `refreshAppearance()`，MainWindowController 主题监听也调用 grid。

- [ ] **Step 6: 构建验证 + Commit**

```bash
git add FlowFinderNative/FlowFinderNative/Bridge/ThumbnailManager.swift FlowFinderNative/FlowFinderNative/UI/FileGridView.swift FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift
git commit -m "fix: [F10-9] 网格视图照搬访达+缩略图修复（问题7/10，修正F7/F8遗漏）"
```

---

## 第五层：功能接线修复

### Task 10: 设置窗口 + 搜索 + 在另一侧打开 + 右键菜单 + 查重重命名（问题3/11/13/15/16/17）

**Files:**
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/MainWindowController.swift`（OpenSettings 通知、查重重命名覆写、右键菜单日志）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/PaneToolbar.swift`（搜索实时）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/Model/PaneState.swift`（搜索基于原始 files）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileListView.swift`（在另一侧打开菜单项）
- Modify: `flowfinder-native/FlowFinderNative/FlowFinderNative/UI/FileGridView.swift`（在另一侧打开菜单项）

- [ ] **Step 1: 注册 OpenSettings 通知观察者（问题3）**

打开 `UI/MainWindowController.swift`，在 `setupNotifications()` 中添加：
```swift
// 任务 F10-10: 注册 OpenSettings 通知（v0.6.6）
// 修复问题3：侧边栏齿轮按钮发的通知无观察者
NotificationCenter.default.addObserver(
    self, selector: #selector(handleOpenSettings(_:)),
    name: NSNotification.Name("OpenSettings"), object: nil
)
```

添加 handler：
```swift
@objc private func handleOpenSettings(_ notification: Notification) {
    SettingsWindowController.shared.showWindow()
}
```

- [ ] **Step 2: 搜索实时生效（问题11）**

打开 `UI/PaneToolbar.swift`，让 PaneToolbar 实现 `NSTextFieldDelegate`：
```swift
// 任务 F10-10: 搜索实时生效（v0.6.6）
// 修复问题11：NSTextField 无 delegate，searchChanged 仅 Enter 触发
extension PaneToolbar: NSTextFieldDelegate {
    func controlTextDidChange(_ obj: Notification) {
        searchChanged()
    }
}
```

在 searchTextField 创建处设置 `searchTextField.delegate = self`。

- [ ] **Step 3: 搜索基于原始 files（问题11 辅助）**

打开 `Model/PaneState.swift`，修复 `applyFilter` 二次过滤隐患：搜索基于原始 files 而非已过滤子集。新增 `allFiles` 保存原始列表，`applyFilter` 从 `allFiles` 过滤。

- [ ] **Step 4: 查重/批量重命名补实现（问题17）**

打开 `UI/MainWindowController.swift`，在 `PaneToolbarDelegate` 扩展中补两方法：
```swift
// 任务 F10-10: 补实现查重/批量重命名回调（v0.6.6）
// 修复问题17：协议默认空实现未被覆写
func paneToolbarDidClickDedupScan(_ toolbar: PaneToolbar) {
    DuplicateScanWindowController.shared.showWindow()
}

func paneToolbarDidClickBatchRename(_ toolbar: PaneToolbar) {
    // 转发到现有 menuBatchRename 逻辑
    let selected = activePaneViewModel.selectedFiles
    guard selected.count >= 2 else {
        // 提示需选中至少2个文件
        return
    }
    menuBatchRename(nil)
}
```

- [ ] **Step 5: 新增"在另一侧打开"菜单项（问题13）**

打开 `UI/FileListView.swift` 和 `FileGridView.swift`，右键菜单新增"在另一侧打开"项（仅文件夹显示）：
```swift
// 任务 F10-10: 新增"在另一侧打开"菜单项（v0.6.6）
// 仅文件夹显示，点击导航另一侧面板到该文件夹
if let entry = clickedEntry, entry.isDirectory {
    let openOtherItem = NSMenuItem(title: "在另一侧打开", action: #selector(openInOtherPane(_:)), keyEquivalent: "")
    openOtherItem.image = NSImage(systemSymbolName: "rectangle.split.2x1", accessibilityDescription: "在另一侧打开")
    openOtherItem.target = self
    menu.addItem(openOtherItem)
}
```

实现 `openInOtherPane(_:)` action，发通知给 MainWindowController，调用对侧 viewModel 切换路径。

- [ ] **Step 6: 右键菜单日志 + path 空修复（问题15/16）**

在 `copyToOtherPane`/`moveToOtherPane`/`showInfoMenu` 入口加日志：
```swift
@objc private func copyToOtherPane(_ sender: NSMenuItem) {
    print("[F10-10] copyToOtherPane clicked, clickedEntry=\(clickedEntry?.path ?? "nil")")
    // ... 原有逻辑
}
```

修复 `handleFileListShowInfo` 在 path 为空时静默返回（给出提示或回退到 selectedFiles.first）。

- [ ] **Step 7: 构建验证 + Commit**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|BUILD)"
```

Expected: `** BUILD SUCCEEDED **`

```bash
git add -A
git commit -m "fix: [F10-10] 功能接线修复-设置/搜索/在另一侧打开/右键菜单/查重重命名（问题3/11/13/15/16/17）"
```

---

## 第六层：视觉打磨

### Task 11: 整体视觉 + 夜间模式克隆访达（问题19/20）

**Files:**
- Modify: 全局 UI 文件（材质、间距、字号统一）

- [ ] **Step 1: 材质统一对齐访达**

检查并统一各区域材质：
- 侧边栏：`.sidebar`（已设置，保留）
- 工具栏：`.headerView` 或 `.windowBackground`
- 文件列表区：透明透出 `.underWindowBackground`
- 详情栏：对齐 `.sidebar`（修正材质不统一）

- [ ] **Step 2: 间距字号对齐访达**

- 侧边栏项目 28pt 行高、8pt 内边距
- 文件列表 26pt 行高
- 工具栏按钮紧凑排列
- 字号：侧边栏 13pt、文件列表 12pt、列头 11pt

- [ ] **Step 3: 夜间模式对比度检查**

深色模式下检查透明背景 + 玻璃材质对比度。若不足，调整透明度。确保 labelColor/secondaryLabelColor 正确解析。

- [ ] **Step 4: 构建验证 + Commit**

```bash
git add -A
git commit -m "style: [F10-11] 整体视觉+夜间模式克隆访达（问题19/20）"
```

---

## 收尾

### Task 12: 整体验收构建 + 版本升级 + 日志

- [ ] **Step 1: Debug 构建**

```bash
export DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer
cd /Volumes/Iris-Data/Download/AI/文件管理系统/flowfinder-native
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Debug build 2>&1 | grep -E "(error:|warning:|BUILD)"
```

- [ ] **Step 2: Release 构建**

```bash
xcodebuild -project FlowFinderNative/FlowFinderNative.xcodeproj \
           -scheme FlowFinderNative \
           -configuration Release build 2>&1 | grep -E "(error:|BUILD)"
```

- [ ] **Step 3: 版本号升级至 0.6.6 (660)**

`project.pbxproj` 中 `MARKETING_VERSION = 0.6.5` -> `0.6.6`，`CURRENT_PROJECT_VERSION = 650` -> `660`（replace_all）。

- [ ] **Step 4: 追加 v0.6.6 变更总结到 MIGRATION_LOG.md**

- [ ] **Step 5: Commit + 呈交用户验收**

```bash
git add -A
git commit -m "chore: v0.6.6 版本升级 + 变更日志"
```

向用户报告 20 个问题修复完成，请截图验收。

# v0.6.5 全量 UI 巡检报告

**巡检日期：** 2026-07-27
**巡检方式：** 代码级核对（对照 macOS 原生访达标准）
**巡检范围：** 14 个 UI 窗口/面板 + 右键菜单 + 主菜单
**前置：** v0.6.5 F2-F8 修复已完成（Task 1-7）

## 巡检汇总

| 组别 | P0 | P1 | P2 | 小计 |
|------|----|----|----|----|
| 主窗口组 | 3 | 10 | 8 | 21 |
| 工具窗口组 | 3 | 8 | 10 | 21 |
| 对话框与菜单组 | 3 | 8 | 8 | 19 |
| **合计** | **9** | **26** | **26** | **61** |

## P0 问题清单（本轮建议修复）

### 主窗口组 P0

1. **[P0] 网格视图缺失 Enter 内联重命名** - `FileGridView.swift` 全文无 keyDown 重写；`FileListView.swift:970-985` 有 Enter 触发 beginInlineRename - 网格视图按 Enter 无响应 - FileGridView 增加 keyDown，Enter 触发重命名
2. **[P0] 网格视图右键菜单项全部缺失 SF Symbol 图标** - `FileGridView.swift:206-227` 14 个菜单项全部无 .image；对照 FileListView 同名菜单项均带图标 - 逐项补齐图标
3. **[P0] 详情栏收起态名称字号在 refresh() 中被动态重写为 13pt，与 setupUI 初始 11pt 不一致** - `ExpandableDetailsBar.swift:112`(11pt) vs `:377`(13pt medium) - 统一字号标准

### 工具窗口组 P0

4. **[P0] FileInfoWindowController 整类为死代码** - `FileInfoWindowController.swift:8` - "显示简介"实际只发 ExpandDetailsBar 通知，独立 Info 窗口从未实例化 - 要么删除要么接线
5. **[P0] 搜索面板 scopePopup 未接线** - `SearchPanelController.swift:368-373` 创建并绑定 action，但 applyFiltersAndReload 未读取选中值 - 范围切换无作用 - 接线或移除
6. **[P0] 关于窗口版本号硬编码 "0.6.5 (650)"** - `AboutWindowController.swift:34` - 未读 CFBundleShortVersionString - 改为读 Bundle.main.infoDictionary

### 对话框与菜单组 P0

7. **[P0] FileGridView 右键菜单完全缺失 SF Symbols 图标**（与主窗口组 P0#2 重复，同一问题）- `FileGridView.swift:206-227`
8. **[P0] FileGridView「复制到/移动到另一面板」未按面板方向区分箭头图标** - `FileGridView.swift:214-215` - 无 panelSide 属性、无 menuNeedsUpdate 动态更新 - 增加 panelSide + menuNeedsUpdate
9. **[P0] ProgressDialog「取消」按钮不取消任务** - `ProgressDialog.swift:40-49` - 仅关闭窗口，未调用 TaskSchedulerManager.cancelTask - 增加 taskId 注入 + cancelTask 调用

**P0 去重后实际为 8 项**（P0#2 与 P0#7 重复）

## P1 问题清单（下一轮修复，共 26 项，此处摘要）

### 主窗口组 P1（10 项）
- 网格视图未同步 appearance 刷新机制
- 网格视图滚动条未用 FFScroller
- 网格视图缩略图无路径校验
- 侧边栏主题切换按钮未订阅 onModeChanged
- 侧边栏工具按钮（hammer）空实现死按钮
- 侧边栏工具栏 fillEqually 拉伸图标按钮
- 路径栏同级目录菜单坐标系错误
- 路径栏首段无同级目录分隔符
- 详情栏材质 .headerView 与侧边栏 .sidebar 不统一
- 详情栏缺权限字段且无独立简介入口

### 工具窗口组 P1（8 项）
- 菜单栏"关于"调用系统面板与自研窗口双重入口
- 设置-关于与独立关于窗口信息不一致
- 查重选项（方式/类型/最小大小）未接入 startScan
- 查重 clearSelection 逻辑缺陷
- BatchRename/FileInfo 未接入玻璃架构
- AppearanceSettingsView onModeChanged 累积泄漏
- AppearanceSettingsView 主题按钮选中无视觉反馈
- 设置侧边栏行高 32 与令牌 24 不一致且无选中高亮

### 对话框与菜单组 P1（8 项）
- FFModalSheet 系列未实现回车确认/Esc 取消
- FileListView 与 FileGridView 菜单结构与顺序不一致
- FileGridView 右键菜单无 validateMenuItem 校验
- ProgressDialog 无任何调用方实例化（死代码）
- TaskPanelWindowController「清除已完成」不实际清除
- TaskPanelWindowController 工具栏无图标
- DeleteConfirmDialog「不再询问」标志未被读取
- TagSelectorDialog 推荐药丸用文本字符模拟圆点

## P2 问题清单（长期优化，共 26 项，略）

详见各组的完整巡检结果。

## 巡检结论

P0 共 8 项（去重后），建议本轮修复。P1/P1 共 52 项纳入下一轮。

P0 问题集中在：
1. **网格视图与列表视图的不对称**（P0#1/2/8 + 多个 P1）- FileGridView 未同步列表视图的 F7/F8/菜单图标修复
2. **死代码与未接线控件**（P0#4/5/9 + P1 ProgressDialog）- FileInfoWindow/scopePopup/ProgressDialog 等功能未接入
3. **硬编码与版本脱钩**（P0#6）- 关于窗口版本号
4. **状态机不完整**（P0#3）- 详情栏字号分支


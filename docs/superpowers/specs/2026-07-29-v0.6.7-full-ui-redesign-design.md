# v0.6.7 全应用 UI 重设计

> **日期：** 2026-07-29
> **版本：** v0.6.7（计划）
> **前置版本：** v0.6.6 (660)
> **状态：** 待用户审阅

---

## 1. 背景

v0.6.6 验收发现 14 个问题，核心根因是"全透明玻璃架构"导致透明度看不清、性能卡顿、设计不统一。用户要求全应用所有窗口重做，从架构层面解决。

## 2. 方向决策

| 决策点 | 选择 |
|--------|------|
| 重设计范围 | 全应用所有窗口重做 |
| 材质架构 | 主窗口侧边栏+边框用亚克力（vibrancy），其他全部实体背景 |
| 操作区背景 | 访达实际色调（日间#F5F5F5/夜间#2D2D2D） |
| 设计方式 | 仿访达原生实现（AppKit 原生组件，不依赖 web skill） |
| 收藏夹 | 命名"个人收藏"，不折叠，直接列出所有文件夹 |
| 标签点击 | 筛选当前活动面板，再次点击取消 |
| 设备栏 | 浮动左下角+实体背景（日间白/夜间黑） |
| 设置页 | 实体背景+分区布局（仿系统偏好设置） |
| 查重页 | 实体背景解决透明度 |
| 详情栏图标 | 空白占位图标，选中后替换 |
| 搜索范围 | 当前目录+子目录本地过滤 |
| 路径栏 | 完全仿访达（固定位置、每段可点击、分隔符弹同级） |
| 分组 | 重新设计仿访达（粘性 header、缩进、计数徽章） |
| 复制/移动反馈 | 底部状态栏进度条（不弹窗） |
| 卡顿 | 排查点击选中/打开+切换目录（缩略图异步/主线程阻塞） |

## 3. 核心架构转变

**从"全透明玻璃架构"转向"亚克力侧边栏 + 实体内容区"架构：**

```
+----------------------------------------------------------+
| 窗口边框（亚克力 vibrancy）                                |
| +----------------+-----------------------+--------------+ |
| | 侧边栏（亚克力） | 操作区（实体#F5F5F5） | 操作区（实体） | |
| | - 个人收藏      | - 工具栏（双行）       | - 同左        | |
| | - 标签          | - 文件列表            |              | |
| | - 底部按钮      | - 详情栏              |              | |
| | (设备浮动左下)  | - 底部状态栏(进度)    |              | |
| +----------------+-----------------------+--------------+ |
+----------------------------------------------------------+
```

- **主窗口侧边栏 + 窗口边框**：NSVisualEffectView `.sidebar` material（亚克力）
- **主窗口操作区**：实体背景，日间 `#F5F5F5`、夜间 `#2D2D2D`
- **所有子窗口**：实体背景，移除 FFGlassView/NSVisualEffectView 透明材质
- **移除 FFGlassView 依赖**：替换为普通 NSView + 实体背景色
- **窗口架构**：保留 titlebarAppearsTransparent（红绿灯浮在侧边栏上方）

## 4. 主窗口重设计

### 4.1 侧边栏（亚克力材质）

- 顶部：应用图标（空白占位风格）+ "FlowFinder" 名称，红绿灯浮在上方
- "个人收藏" section：不折叠，直接列出所有收藏文件夹，彩色真实图标，左对齐
- "标签" section：紧跟个人收藏下方，每行彩色圆点+完整标签名，点击筛选当前面板
- 侧边栏底部：主题切换/设置/工具按钮（紧凑排列）

### 4.2 操作区（实体背景）

- 双面板布局保留（FlowFinder 特性）
- 工具栏（双行）：
  - Row1：后退/前进/上一级/刷新 + 路径栏（完全仿访达）
  - Row2：搜索框 + 排序 + 分组 + 视图切换 + 工具按钮
- 文件列表：列宽自动、药丸列头、分组仿访达、选中强调蓝、缩略图异步
- 详情栏：空白占位图标，选中后替换

### 4.3 路径栏（完全仿访达）

- 固定位置（不再随布局变更）
- 每段路径可点击跳转
- 分隔符可点击弹出同级目录下拉
- 当前位置高亮

### 4.4 分组（重新设计仿访达）

- 粘性 section header（滚动时固定顶部）
- 分组内文件缩进
- section header 显示计数徽章（如"图片 12"）
- 修复 Title 重叠 bug

### 4.5 设备栏（浮动左下角+实体背景）

- 浮动在窗口左下角，实体背景（日间白/夜间黑）
- 展开显示所有设备，不需滚动
- 与侧边栏视觉分离

### 4.6 底部状态栏

- 新增：显示复制/移动进度条（不弹窗）

## 5. 子窗口重设计（全部实体背景）

| 窗口 | 重设计方案 |
|------|-----------|
| 设置窗口 | 实体背景+分区布局（仿系统偏好设置：左侧分区列表+右侧内容） |
| 查重扫描窗口 | 实体背景，扫描进度/分组列表/预览清晰可读 |
| 搜索面板 | 实体背景，scopePopup 接线 |
| 关于窗口 | 实体背景，版本号动态读取 |
| 文件信息窗口 | 实体背景，仿访达 Get Info |
| 批量重命名窗口 | 实体背景，规则配置+预览 |
| 对话框 | 实体背景，移除 FFGlassView |
| ProgressDialog | 实体背景（底部状态栏补充） |

所有子窗口移除 FFGlassView 依赖，统一实体背景。

## 6. 功能修复

### 6.1 问题1 - 复制/移动到另一侧面板 + 底部进度条

- 修复 action 接线（确认是否触发）
- 新增底部状态栏进度条组件
- 复制/移动时显示进度，完成后自动隐藏

### 6.2 问题3 - 标签点击筛选当前面板

- 标签行添加点击 action
- 点击后设置当前活动面板筛选条件为该标签
- viewModel 过滤显示有该标签的文件
- 再次点击取消筛选

### 6.3 问题7 - 卡顿修复（点击选中/打开 + 切换目录）

- 缩略图加载完全异步（不阻塞主线程）
- `NSWorkspace.shared.icon(forFile:)` 移到后台队列
- cell 复用优化
- 切换目录先显示列表再异步加载缩略图

### 6.4 问题11 - 搜索修复（当前目录+子目录）

- 搜索基于原始 allFiles（非已过滤子集）
- 支持子目录递归搜索
- 实时过滤（controlTextDidChange）

### 6.5 问题12 - 路径栏完全仿访达

- 固定位置
- 每段可点击跳转
- 分隔符弹同级下拉
- 当前位置高亮

### 6.6 问题14 - 分组重新设计仿访达

- 粘性 section header
- 分组内缩进
- 计数徽章
- 修复 Title 重叠

## 7. 验收标准

| # | 问题 | 验收标准 |
|---|------|---------|
| 1 | 复制/移动 | 点击生效，底部状态栏显示进度 |
| 2 | 收藏夹 | "个人收藏"，不折叠，直接列出 |
| 3 | 标签点击 | 点击筛选当前面板，再次点击取消 |
| 4 | 设备栏 | 浮动左下角实体背景，日间夜间清晰 |
| 5 | 设置页 | 实体背景+分区布局，内容清晰可读 |
| 6 | 查重页 | 实体背景，内容清晰可读 |
| 7 | 卡顿 | 点击选中/打开、切换目录流畅 |
| 9 | 操作区背景 | 日间#F5F5F5/夜间#2D2D2D 实体 |
| 10 | 详情栏图标 | 空白占位，选中后替换 |
| 11 | 搜索 | 当前目录+子目录，实时过滤，不空白 |
| 12 | 路径栏 | 固定位置、每段可点击、分隔符弹同级 |
| 14 | 分组 | 粘性 header、缩进、计数徽章，无重叠 |
| 整体 | 视觉 | 全应用实体背景统一，仅侧边栏亚克力 |

## 8. 不在本设计范围内

- Rust Core / Bridge 层修改
- v0.6.6 巡检的 P1/P2 问题（除被本轮覆盖的）
- Intel 通用二进制
- AI 标签生成引擎

## 9. 风险与缓解

| 风险 | 缓解 |
|------|------|
| 移除 FFGlassView 涉及大量文件 | 分窗口逐步替换，每个窗口独立构建验证 |
| 卡顿根因可能不止缩略图 | 用 systematic-debugging 逐个排查 |
| 路径栏完全重写可能引入新 bug | 参照访达行为逐项实现，每步验证 |
| 分组粘性 header 在 NSTableView 无原生支持 | 自定义滚动监听实现 |
